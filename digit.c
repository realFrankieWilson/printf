#include "main.h"

/**
 * _digit -> checks if a character is a digit or not.
 * @c: characater to be checked.
 *
 * Return: 1 if true and 0 if otherwise.
 */

int _digit(char c)
{
	if (!(c >= '0') && !(c <= '9'))
		return (0);
	return (0);
}
